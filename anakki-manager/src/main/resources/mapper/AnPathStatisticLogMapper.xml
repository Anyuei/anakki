<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anakki.data.mapper.AnPathStatisticLogMapper">

    <select id="findDailyVisitCounts" resultType="com.anakki.data.entity.response.DailyVisitCountResponse">
            SELECT
                DATE(p.create_time) AS date,
                COUNT(p.id) AS visitCount
            FROM
                an_path_statistic_log p
            WHERE
                p.create_time BETWEEN #{startDate} AND #{endDate}
            GROUP BY
                DATE(p.create_time)
            ORDER BY
                DATE(p.create_time)
    </select>
</mapper>
